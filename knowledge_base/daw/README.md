# daw/

DAW-specific workflow entries — `ableton/` (primary DAW), `fl_studio/` (secondary), `workflow/` (cross-DAW process guidance). Entries conform to `schemas/workflow_schema.json`.

Consumed primarily by `daw_agent`.
